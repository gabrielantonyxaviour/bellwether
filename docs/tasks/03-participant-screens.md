# Task 03 — blk_ui_participant (worker: bw-participant, gpt-6-sol)
Owns: web/src/routes/app/**, web/src/lib/wallet-ui/**, web/src/components/trade/**, tests/e2e/participant.spec.ts. Fork ports 8970/8971 for its e2e.
Screens: pg_onboard, pg_trade, pg_liquidity (see spec.json surfaces for the updated contracts).
- Onboarding: start from ReUI onboarding-1: `pnpm -C web dlx shadcn@latest add @reui/onboarding-1`. Flow: connect (Wallet UI) → sign the credential service's one-time challenge (GET /admit/challenge, POST /admit {wallet,message,signature} — being added by bw-integration; if not merged yet, code to that contract) → screening → admitted card; rejected SANCTIONED/REVOKED; retry on SCREENING_UNAVAILABLE.
- Trade: a real trading terminal like OKX/Jupiter stock pages (see Gabriel's reference: header stats row, big candlestick chart, Trades/Liquidity/Positions/Orders tabs, right-rail buy/sell panel with amount presets, Tokenized info panel). REAL data only: underlying FWDI daily/intraday candles from the keyless Yahoo chart API (https://query1.finance.yahoo.com/v8/finance/chart/FWDI?range=6mo&interval=1d, plain 'Mozilla/5.0' UA) proxied through services/api if CORS blocks (coordinate: add a route under services/api only if bw-integration has finished; otherwise add web/src/lib/market.ts + a tiny proxy note), onchain pool price/liquidity/24h volume from GET /symbols and the pool account, Trades tab from GET /explorer data (/tape endpoint), Positions from the wallet's token balances + LpPosition, venue status panel from SymbolRecord (halt, heartbeat age, share budget) — chart library: lightweight-charts from cdn/npm. Buy/sell panel uses SolanaUI parts (pnpm -C web dlx shadcn@latest add https://www.solanaui.dev/r/<component>.json — check solanaui.dev docs for exact registry names) with our own review → awaiting signature → pending → success flow via @/lib/tx; blocked states show the program refusal.
- Liquidity: modelled on Orca/Meteora/Raydium deposit/withdraw panels (captures in ux-flows/liquidity), pool stats, positions, non-transferable badge, halted = withdraw only.
Pitch mockups with every state and interaction (the approved direction under yolo): https://claude.ai/artifact/5qQ23wdMqWsZnrApnDKbiX — local copy /private/tmp/claude-501/-Users-gabrielantonyxaviour-Documents-products-abel/97892003-d6e6-4c99-8e8d-ea0ec34eadb1/scratchpad/pitch/index.html (the SCREENS render functions show exact copy, states and click flows), real product captures with action→reaction notes in /private/tmp/claude-501/-Users-gabrielantonyxaviour-Documents-products-abel/97892003-d6e6-4c99-8e8d-ea0ec34eadb1/scratchpad/ux-flows/manifest.json. Web shell: web/README.md (routes, @/lib/api, @/lib/program, @/lib/tx, @/lib/wallet, @/lib/cluster). Wallet: implement the Wallet UI provider (https://registry.wallet-ui.dev) behind the existing BellwetherWallet interface if the screen needs connect (first worker to need it does it in web/src/lib/wallet-ui/**; others import). Functional look only (neutral shadcn theme, no brand tokens). NO mock data: every number comes from chain, the services, or real market APIs; when a service is down show an honest empty/error state. Run the services locally against a Surfpool mainnet fork for development (docs/tasks/01 brief / scripts/fork when ready). Accept command and artifact are in spec.json for your block; the Playwright test must exercise real on-chain states on the fork. Check each page at 375/768/1440 px.
Verify: blk_ui_participant.

## Final worker report — 2026-09-25

- **Block and scope:** `blk_ui_participant` covers `/app/onboard`, `/app/trade/:symbol`, and `/app/liquidity/:symbol`. The ReUI `@reui/onboarding-1` stepper/header was installed with the licensed shadcn registry command; `web/components.json` uses `REUI_LICENSE_TOKEN` from gitignored `web/.env.local`. SolanaUI registry components supply token amount input, address display, and transaction toast/status. No registry secret was committed.
- **Real behavior:** Onboarding signs the issuer's one-time challenge and resolves admitted, refused, screening error, and retry states. Trade uses live Yahoo FWDI candles through `/market/FWDI`, onchain trade markers, onchain pool/venue/budget data, real Trades/Liquidity/Positions/Orders tabs, quote/review/confirmation, and program refusal labels. Liquidity reads real balances and LP positions and supports deposits, withdrawals, and halted withdrawal. Participant chain reads batch accounts, poll, and retry 429/5xx with backoff through the keyed `/rpc` gateway; the browser has no websocket dependency.
- **Live QA fixes:** Connected credentials resolve to admitted, not admitted, or an error with retry instead of remaining on “Checking…”. Mobile trade prints use cards at 375 px, chart markers no longer label over the plot, and balance/wallet controls have room to wrap. Trade input blur or a successful amount clear no longer erases the transaction result.
- **Admission evidence follow-up:** On admitted and revisit states, “How you were admitted” reads the issuer's live `/health` SDN publish date, official digital-currency address count, fetch time, source URL, stale flag, and separate rehearsal-only count. It links the live SAS program, credential, schema, wallet attestation, admission transaction, and thawed stock account to Solscan, with the credential expiry and “Test admission, not KYC” framing. The issuer's `admissionSignature` status field (bw-integration `43e495a`) retains the transaction on revisit; that same transaction contains SAS issue and Token-2022 thaw. If list health fails, the panel shows an error and Retry. Swap, LP deposit, and LP withdrawal transaction links all include `?cluster=devnet`.
- **Checks:** `pnpm -C web build` and typecheck passed; Vitest passed 45/45. The isolated Surfpool mainnet fork test passed 1/1: signed admission, buy, indexed tape, LP deposit, halted withdrawal, and onchain halted/stale/cap/paused/revoked refusals. `tests/e2e/participant-devnet.spec.ts` passed 6/6 against the deployed devnet program and live API: a browser Wallet Standard signer broadcast a signed buy through `/rpc`, its signature appeared on the public tape, then LP deposit and full withdrawal confirmed. It also verified admission evidence and reload, SDN health failure/retry, admitted, fresh unadmitted, issuer error/retry, Solscan devnet links, and 375/768/1440 layouts. After the final evidence copy edit, the focused live evidence tests passed 2/2 and build passed. The test's localhost issuer route forwards actual live responses; it supplies no synthetic admission data.
- **Visual QA and deployment:** Inspected trade, liquidity, and the admitted-wallet evidence panel in Brave at 375, 768, and 1440 px. All settled with real chain and issuer data, with no clipped controls, horizontal overflow, or overlapping chart markers. bw-fork deployed final admission-panel source `602c174` from clean HEAD to `https://bellwether.larinova.com` and `https://16586ee2.bellwether-e1y.pages.dev`; its public URL QA passed 24/24 route-width checks.
- **Harness:** Final `--provider codex --wait` returned `blk_ui_participant     passed` and `pass  npx playwright test tests/e2e/participant.spec.ts` after admission evidence commits `76a857d` and `602c174`.
- **Handoff:** Commits `822c817`, `369c267`, `e8c3d67`, `53a5a2a`, `4a8c6c6`, `76a857d`, `602c174`. No mainnet transaction or deployment was performed by this worker. No Gabriel decision is needed for this block. [Circuit](http://localhost:4179/work/products/prod_081357ef-1e52-421c-ad10-8a03a624f889/circuit?v=1.0.0).

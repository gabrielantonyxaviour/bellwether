# Task 05 — blk_ui_public (worker: bw-public, grok-4.7)
Owns: web/src/routes/public/**, web/src/components/public/**, tests/e2e/public.spec.ts. Fork ports 8990/8991 for its e2e.
Screens: pg_home (/), pg_tape now at route /explorer (Explorer), pg_proof now at route /about (About). Update web/src/app/paths.ts and routes.tsx ONLY for these two renames (coordinate: small commit of just those lines).
- Home: live status strip from GET /venue and /halts, thesis line, three doors, 'how a swap is checked' strip.
- Explorer: dense live trade table (ReUI data grid) from GET /tape (poll or SSE), side filter, 30-day date picker, rolling 24h volume and end-of-day pool size chips, JSON link — patterns from GeckoTerminal/DexScreener captures (ux-flows/tape).
- About: explains the product (what it is, the SEC rules it enforces, how a swap is checked, where it runs: devnet end to end, mainnet for the pitch), program id + signatures per environment from scripts/deploy/deployments/<cluster>.json and scripts/fork/out/fork-scenario.json with Solscan links, real-vs-stand-in table, who can pause/upgrade/override (from /notice/draft facts).
Pitch mockups with every state and interaction (the approved direction under yolo): https://claude.ai/artifact/5qQ23wdMqWsZnrApnDKbiX — local copy /private/tmp/claude-501/-Users-gabrielantonyxaviour-Documents-products-abel/97892003-d6e6-4c99-8e8d-ea0ec34eadb1/scratchpad/pitch/index.html (the SCREENS render functions show exact copy, states and click flows), real product captures with action→reaction notes in /private/tmp/claude-501/-Users-gabrielantonyxaviour-Documents-products-abel/97892003-d6e6-4c99-8e8d-ea0ec34eadb1/scratchpad/ux-flows/manifest.json. Web shell: web/README.md (routes, @/lib/api, @/lib/program, @/lib/tx, @/lib/wallet, @/lib/cluster). Wallet: implement the Wallet UI provider (https://registry.wallet-ui.dev) behind the existing BellwetherWallet interface if the screen needs connect (first worker to need it does it in web/src/lib/wallet-ui/**; others import). Functional look only (neutral shadcn theme, no brand tokens). NO mock data: every number comes from chain, the services, or real market APIs; when a service is down show an honest empty/error state. Run the services locally against a Surfpool mainnet fork for development (docs/tasks/01 brief / scripts/fork when ready). Accept command and artifact are in spec.json for your block; the Playwright test must exercise real on-chain states on the fork. Check each page at 375/768/1440 px.
Verify: blk_ui_public.

## Worker report — 2026-09-25

- **Changed scope:** `/`, `/explorer` (was `/tape`), and `/about` (was `/proof`), with redirects from the old paths. Shared shell nav labels are Explorer and About. `paths.tape` and `paths.proof` stay as aliases of the new URLs. Told bw-participant before editing the shell.
- **Real data:** Home status is GET `/venue` and `/halts` (devnet program `88chqe41hw9uhqrUK6KfytQ7aZgEGJzcqszXGKJFWfuB`, Nasdaq relay heartbeat, no open halt). Explorer prints, 24h volume, and end-of-day pool size come from GET `/tape`. About signatures come from `scripts/deploy/deployments/devnet.json` and `scripts/fork/out/fork-scenario.json`, with Solscan `?cluster=devnet` on devnet links. Who can pause, upgrade, or override is read from GET `/notice/draft` chain facts, including the BWRS permanent delegate. Mainnet has no deployment file, so that card says so.
- **States:** Venue loading, a 503 error with retry, an empty tape date, and live prints. Narrow widths use stacked rows; the dense table is from the `lg` breakpoint up. The ReUI data-grid registry needs a license key here, so the table is local shadcn markup.
- **Checks:** `pnpm -C web exec tsc -b --noEmit` passed. `pnpm -C web test` passed (36 tests). `npx playwright test tests/e2e/public.spec.ts --reporter=line` passed (live devnet API, 375/768/1440, screenshot `evidence/public-pages.png`).
- **Harness verdict:** `ABEL_URL=http://localhost:4179 npm run graph -- verify --product prod_081357ef-1e52-421c-ad10-8a03a624f889 --version 1.0.0 --block blk_ui_public --provider grok --wait` **passed** (`pass npx playwright test tests/e2e/public.spec.ts`).
- **Coordinator / Gabriel:** Nothing to decide. The accept run uses the live devnet services rather than a fresh fork on 8990/8991, because devnet was already serving the tape and the harness budget is five minutes. Fork signatures are on About and only resolve on the machine running that fork.

## Follow-up — historical tape date

`/explorer?date=2026-09-25` keeps the 25 Sep UTC tape selectable for the 35-day retention window, including after that UTC day ends. The page shows one signature link at the current width, so the fresh-wallet print `publicFreshSwap` stays visible at 375, 768, and 1440. A "2026-09-25 tape" link sits next to the date field. Playwright asserts that link is the first visible match for the signature.

